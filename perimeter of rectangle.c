#include<stdio.h>
int main()
{
	int l,b,perimeter;
	printf("enter the values of l,b");
	scanf("%d%d",&l,&b);
	perimeter=2*(l+b);
	printf("perimeter is %d",perimeter);
	getch();
	return 0;
	
}
